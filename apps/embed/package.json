{
    "name": "@flowcanvas/embed",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "esbuild src/loader.ts --bundle --minify --outfile=../../apps/web/public/loader.js"
    },
    "devDependencies": {
        "esbuild": "^0.20.1",
        "typescript": "^5.3.3"
    }
}
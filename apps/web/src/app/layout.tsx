import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "FlowCanvas | Next-Gen User Onboarding & Product Tours",
  description: "Build beautiful, high-converting product tours and user onboarding flows with our visual canvas editor. Zero code required.",
  openGraph: {
    title: "FlowCanvas | Build Product Tours Faster",
    description: "The visual canvas editor for modern product teams.",
    url: "https://flowcanvas.dev",
    siteName: "FlowCanvas",
    images: [
      {
        url: "/og-image.png",
        width: 1200,
        height: 630,
      },
    ],
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
